twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

    globals:
        id_analitics: '%env(id_analitics)%'
        habilitar_captcha: '%env(habilitar_captcha)%'